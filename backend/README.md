# MATA SE26 — Backend API

Deploy ke Railway:
1. Push folder ini ke GitHub repository baru
2. Railway → New Project → Deploy from GitHub → pilih repo ini
3. Set environment variable: MONGODB_URI
4. Setelah deploy, jalankan seed: railway run node seed.js
